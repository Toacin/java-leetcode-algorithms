package LeetCode.Medium;

public class PalindromicSubstring {
    public static int countSubstrings(String s) {
        int palindromeCount = s.length();
        for (int i=0; i<s.length(); i++) {
            int j=i-1;
            int k=i+1;
            while(j>=0 && k<=(s.length()-1) && s.charAt())
        }
    }
}
